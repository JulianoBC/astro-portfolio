---
import ComponentUnderTest from './Header.astro';
import { getComponentOutput } from 'astro-component-tester';
import '@testing-library/jest-dom';
import { expect, test } from 'vitest';



test('Header component renders correctly', async () => {
  // Render the Header component
  const props = {};
  const component = await getComponentOutput('./src/components/header/Header.astro', props);

  expect(component.html).toContain("<header>");

});